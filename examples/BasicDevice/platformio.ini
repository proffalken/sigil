; CI build target for the Sigil library. Not needed to *use* the library —
; see the README for adding Sigil via lib_deps in your own project.
[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
lib_deps =
    symlink://../..
    bblanchon/ArduinoJson@^7.0.0
